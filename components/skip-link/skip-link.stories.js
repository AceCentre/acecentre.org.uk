import { SkipLink } from "./skip-link";

export default {
  title: "",
  component: SkipLink,
};

const Template = (args) => <SkipLink {...args} />;

export const Standard = Template.bind({});
Standard.args = {};
